---
description: "Compound card for commerce rows \u2014 image well, overlaid badge, and a title/subtitle/metric footer"
---

import {
  productCardProps,
  productCardImageProps,
  productCardBadgeProps,
  productCardContentProps,
  productCardHeaderProps,
  productCardTitleProps,
  productCardSubtitleProps,
  productCardMetricProps,
} from "./product-card.props";

<ComponentPreview name="product-card-demo" padding="compact" />

## Installation

<InstallTabs slug="product-card" />

## Usage

```tsx
import {
  ProductCard,
  ProductCardContent,
  ProductCardHeader,
  ProductCardMetric,
  ProductCardSubtitle,
  ProductCardTitle,
} from "@/components/ui/product-card";

<ProductCard>
  <ProductCardContent>
    <ProductCardHeader>
      <ProductCardTitle>Twemco Clock</ProductCardTitle>
      <ProductCardSubtitle>Clock</ProductCardSubtitle>
    </ProductCardHeader>
    <ProductCardMetric>$219</ProductCardMetric>
  </ProductCardContent>
</ProductCard>
```

## Basic

<ComponentPreview name="product-card-basic" padding="compact" />

## Inner layout

<ComponentPreview name="product-card-inner" padding="compact" />

## Sizes

<ComponentPreview name="product-card-sizes" padding="compact" />

## Badge

<ComponentPreview name="product-card-badge" padding="compact" />

## Grid

<ComponentPreview name="product-card-grid" padding="compact" />

## Preparing images

The well is `object-contain` over a tinted ground, so a product shot with its
background removed sits in it far better than a rectangular photo — and a
transparent PNG is the only kind that works in both themes, where a
white-background JPEG shows its corners the moment the page goes dark. Every
example on this page uses one.

On macOS you can cut one out without opening an editor: right-click the file in
Finder and choose **Quick Actions → Remove Background**. It writes a new PNG
with transparency beside the original. If it isn't in the menu, open **Quick
Actions → Customize** and add it under Extensions — Apple's [guide to Quick
Actions in the
Finder](https://support.apple.com/guide/mac-help/perform-quick-actions-in-the-finder-mchl97ff9142/mac)
covers both.

## Override Styling

Every part takes its own `className`, merged last through `tailwind-merge`, so a
utility that conflicts with a default replaces it instead of stacking with it.
The card's type size is set once on the root and inherited by the title,
subtitle and metric — and the badge, which is sized in `em` — so one `text-*`
moves the whole block.

<ComponentPreview name="product-card-override-styling" padding="compact" align="top" />

Parts you hold no reference to are reachable by `data-slot`:

| Slot | Element |
| --- | --- |
| `product-card` | The card root |
| `product-card-image` | The square image well |
| `product-card-img` | The `<img>` inside it — or use `imageClassName` |
| `product-card-badge` | The badge pinned over the image |
| `product-card-content` | The row holding the header and the metric |
| `product-card-header` | The title and subtitle group |
| `product-card-title` | The product name |
| `product-card-subtitle` | The secondary line |
| `product-card-metric` | The trailing value |

`size` caps the card's width; a `w-*` or `max-w-*` in `className` overrides that
cap outright, which is what the grid above relies on.

The image well's resting ground and its mouse-enter ground are two separate
utilities. Passing `bg-stone-200` alone leaves the component's own
`hover:bg-muted/80` in charge from the moment the pointer arrives, so recolour
both: `bg-stone-200 hover:bg-stone-300`.

## Accessibility

The root is a `div`, not a control. Passing `onClick` gives it a pointer cursor
but no keyboard access — for a card that must be reachable by keyboard, put a
real `<a>` or `<button>` on the title and stretch it over the card with
`after:absolute after:inset-0`, the way the grid example wraps each card in a
link. That keeps a badge inside the card legal, which nesting a control inside
`role="button"` would not.

## API Reference

### ProductCard

<PropsTable props={productCardProps} />

### ProductCardImage

<PropsTable props={productCardImageProps} />

### ProductCardBadge

<PropsTable props={productCardBadgeProps} />

### ProductCardContent

<PropsTable props={productCardContentProps} />

### ProductCardHeader

<PropsTable props={productCardHeaderProps} />

### ProductCardTitle

<PropsTable props={productCardTitleProps} />

### ProductCardSubtitle

<PropsTable props={productCardSubtitleProps} />

### ProductCardMetric

<PropsTable props={productCardMetricProps} />
